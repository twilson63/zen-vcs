{
  "name": "zen-vcs",
  "version": "0.1.0",
  "description": "Zen Version Control System — a CAP-Tree client for agents",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "zen-vcs": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src/",
    "prepublishOnly": "npm run build"
  },
  "keywords": ["zenbin", "cap-protocol", "version-control", "content-addressed", "ed25519", "lmdb"],
  "author": "Rakis",
  "license": "MIT",
  "dependencies": {
    "node-lmdb": "^0.9.8",
    "commander": "^12.0.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^1.6.0",
    "@types/node": "^20.0.0",
    "eslint": "^9.0.0"
  }
}